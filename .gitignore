# Ignore the local iac project directory (keeps it locally but removes it from the repo/index)
# (removed iac-project/ so folder can be tracked again for Vercel deployment)

# You can add other patterns below
